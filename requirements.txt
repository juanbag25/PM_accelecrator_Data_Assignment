# =============================================================================
# Weather Trend Forecasting — Python dependencies
# =============================================================================
# Recommended install path on Windows (no GPU): use the conda environment
# (see environment.yml). geopandas / pmdarima / prophet pull native libraries
# (GDAL, compilers) that are painful to build with pip on Windows; conda-forge
# ships prebuilt binaries.
#
# Versions below are pinned to what is installed in the `weather-forecasting`
# conda env (Python 3.11). PyTorch must be installed as a CPU build (optional
# section). Run Python via: conda run -n weather-forecasting python ...
# =============================================================================

# --- Core data stack ---
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
pyarrow==24.0.0            # parquet I/O

# --- Visualization ---
matplotlib==3.11.0
seaborn==0.13.2
plotly==6.8.0

# --- Classical time series ---
statsmodels==0.14.6
pmdarima==2.0.4            # conda-forge (works with numpy 2.x here)
prophet==1.3.0            # conda-forge

# --- Gradient boosting (primary model family) ---
lightgbm==4.6.0
xgboost==3.2.0

# --- Feature importance / explainability ---
shap==0.51.0

# --- Climate / geospatial analysis ---
pymannkendall==1.4.3
pycountry-convert==0.7.2
geopandas==1.1.4          # conda-forge (pulls GDAL)
folium==0.20.0

# --- Config / utilities ---
pyyaml==6.0.3
tqdm==4.68.3

# =============================================================================
# OPTIONAL — heavier, only if time allows (model Tiers 3-4). Install on demand.
# =============================================================================
# PyTorch CPU build (required by the deep-learning and foundational models):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch
# neuralforecast          # N-BEATS / NHITS / TFT
# darts                   # alternative TS framework
# autogluon.timeseries    # Chronos (Bolt) zero-shot foundational model
# optuna                  # hyperparameter tuning
# streamlit               # optional dashboard
